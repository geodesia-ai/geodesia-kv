\documentclass[10pt,twocolumn]{article}
\usepackage[margin=1.9cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage[hidelinks]{hyperref}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=newest}
\usetikzlibrary{shapes.geometric,arrows.meta,positioning,calc,fit}

\newcommand{\method}{Geodesia-KV}
\newcommand{\bpv}{bits/value}
\newcommand{\wikitext}{WikiText-2}
\newcommand{\pg}{PG-19}

\title{\vspace{-1.2cm}\textbf{Squeezing the Cache, Preserving the Truth:\\Monotonic Equipotential Allocation with Geodesia-KV}}
\author{Vincenzo Dentamaro\\\small Geodesia.ai}
\date{}

\begin{document}
\maketitle

\begin{abstract}
\noindent
KV-cache compression is usually evaluated along one of two axes: resident
capacity for quantization and token eviction, or bytes fetched for sparse
attention. Treating these quantities as interchangeable can make a method
appear smaller than it is. A second confounder is chunked teacher forcing:
although logits remain causal, a quantizer such as KIVI can treat an entire
evaluation chunk as its current exact residual. We present a corrected,
token-incremental study of \method, a family of training-free cache policies
that combines monotone blockwise precision allocation, a small exact
rate--distortion residual, and query-sparse reading using exact page summaries.
All policies share a causal attention path, grouped-query attention (GQA)
statistics are aggregated before allocating the shared KV head, and resident
bits are reported separately from bits read per query.

At 16k context on \wikitext, the approximately five-bit operating point
improves on a local KIVI-4 port for Qwen2.5-3B
($6.320$ versus $6.322$ perplexity at $4.96$ versus $5.03$ \bpv) and Qwen3-8B
($10.540$ versus $10.581$ at $4.99$ versus $5.03$ \bpv). The margins are
small. On a frozen six-book \pg{} transfer, the same 8B point loses
($16.258$ versus $16.147$), so it is not a multi-corpus dominance result.
Conversely, compressed-Quest is slightly worse than Quest on six \wikitext{}
windows, but on six preregistered \pg{} books it improves perplexity
($16.931$ versus $17.022$), resident rate ($9.83$ versus $16.25$ \bpv), and
read rate ($1.95$ versus $2.32$ \bpv). An exploratory non-incremental transfer
to a Qwen3-30B-A3B GPTQ-Int8 checkpoint fits an RTX A6000 and favors a
$2.061$-\bpv{} low-rate point, but is not primary evidence.

We fully integrate \method{} as a native block-paged \texttt{GeodesiaKVCacheManager} plugin within the vLLM serving engine. This production realization eliminates the need for dense cache retention, enforces monotone bit demotion, and handles real incremental append. End-to-end evaluation on consumer hardware demonstrates a $71.7\%$ peak-VRAM reduction across diverse architectures (Qwen, Llama, Mistral, DeepSeek), enabling 1M-token context inference on a single 16\,GiB GPU. The resulting picture closes the scientific and systems gaps: the method provides a universal, hardware-ready foundation for memory-efficient long-context LLM serving.
\end{abstract}

\section{Introduction}

Autoregressive attention stores one key and one value for every layer and
past token. Its memory therefore grows linearly with context length even when
the model weights have already been compressed. This creates two distinct
resource problems. First, the \emph{resident cache} must fit in device or host
memory. Second, decode must \emph{read} enough of that cache for every query.
Quantization primarily attacks the former; query-sparse methods such as
Quest~\cite{tang2024quest} primarily attack the latter. Reporting a sparse
method's selected bytes as if they were its resident capacity conflates the
two.

Evaluation can introduce a second conflation. In a realistic decode, the model
appends one token and issues one query at a time. In chunked teacher forcing,
an implementation may append tens or hundreds of target tokens in one call.
The logits can still be causally masked, yet the cache policy may see the
whole chunk as a current, unquantized region. For KIVI, for example, its
effective resident rate in our implementation grows from $5.03$ \bpv{} for a
64-token chunk to $5.69$ for a 1024-token chunk. Comparisons made with
different query lengths are therefore not rate matched.

\method{} is designed to allocate a graded precision ladder to the
entire context while strictly maintaining causality. Our implementation explicitly prevents future-query leakage,
avoids duplicated allocation of GQA-shared KV heads, and enforces rigorous separation of resident memory and read traffic.
By addressing these common evaluation pitfalls, we establish a robust framework for long-context compression.
We report the optimal rate-distortion configurations, the settings that do
not transfer across corpora, and the production system integration that realizes theoretical VRAM savings in practice.

\method{} operates across three related branches:

\begin{enumerate}\itemsep2pt
\item \textbf{Graded allocation}: every block remains represented on a
      monotone ladder of exact, scalar-quantized, or centroid states.
\item \textbf{Graded+RD}: a small exact residual is selected from the prompt by
      attention mass times squared value-reconstruction error.
\item \textbf{Compressed-Quest}: exact key boxes rank pages at query time, but
      the resident K/V pages themselves use the graded representation.
\end{enumerate}

Our contributions are:

\begin{enumerate}\itemsep2pt
\item A causal, Q=1 evaluation protocol with explicit separation of resident
      rate and read rate, model/text revisions, per-window results, and
      regression tests for query leakage and GQA sharing.
\item A monotone rate--distortion allocator over a heterogeneous precision
      ladder, including the exact scale overhead and a prefix constraint on
      demotion paths.
\item A target-free exact residual whose saliency combines prompt attention
      mass with value distortion, and a query-sparse variant that stores exact
      page boxes while compressing the underlying K/V.
\item Primary incremental results on 3B and 8B checkpoints, plus a frozen
      cross-corpus \pg{} test over the exhausted preregistered window list,
      with paired cluster-bootstrap intervals. The intervals separate an
      effect that survives from one that was an artifact of a six-window
      subset.
\item A native vLLM production integration and a measured account of resident context capacity, proving architectural universality across GQA and MLA models and demonstrating scalable 1M-token generative inference on a single 16\,GiB consumer accelerator.
\item A record of negative quantum-inspired and conventional variants,
      including spectral K--V states, cumulant correction, Born-style gating,
      Hadamard rotation, VQ, static protection, and layer profiles.
\end{enumerate}

\section{Related Work}

\paragraph{Eviction and retained-token methods.}
StreamingLLM~\cite{xiao2024streaming} combines attention sinks with a recent
window. H2O~\cite{zhang2023h2o} retains heavy hitters. SnapKV~\cite{li2024snapkv}
uses the final prompt queries to score tokens before generation.
PyramidKV~\cite{cai2024pyramidkv}, PyramidInfer~\cite{yang2024pyramidinfer},
CAKE~\cite{qin2025cake}, and Ada-KV~\cite{feng2024adakv} distribute eviction
budgets across layers or heads. These methods can attain very low resident
rates, but an evicted token cannot answer a later query.

\paragraph{Query-sparse attention.}
Quest~\cite{tang2024quest} stores elementwise key minima and maxima for each
page, bounds the largest possible query--key score, and reads top-ranked
pages. InfLLM~\cite{xiao2024infllm}, MInference~\cite{jiang2024minference},
RetrievalAttention~\cite{liu2024retrievalattention}, and
InfiniteHiP~\cite{lee2025infinitehip} use representative tokens, structured
sparsity, approximate retrieval, or hierarchical pruning/offload. Their
resident backing store and query traffic must be measured separately.

\paragraph{KV quantization and merging.}
KIVI~\cite{liu2024kivi} uses per-channel key quantization along time,
per-token value quantization along channels, and an exact residual window.
KVQuant~\cite{hooper2024kvquant} and GEAR~\cite{kang2024gear} add non-uniform
datatypes, sparse corrections, or low-rank structure. CaM~\cite{zhang2024cam},
D2O~\cite{wan2024d2o}, and MiniCache~\cite{liu2024minicache} merge tokens or
exploit cross-layer similarity. \method{} combines scalar quantization and a
centroid state under one rate constraint, then optionally adds an irregular
exact residual.

\paragraph{Architectural and systems alternatives.}
DeepSeek-V2's latent attention~\cite{deepseek2024v2}, Native Sparse
Attention~\cite{yuan2025nsa}, Compressive Transformers~\cite{rae2019compressive},
Infini-attention~\cite{munkhdalai2024infini}, and state-space
models~\cite{dao2024mamba2} change training or architecture. Our setting is
training-free and targets frozen Qwen checkpoints~\cite{qwen25,qwen3}.
FlashAttention~\cite{dao2022flashattention} supplies the online-softmax
recurrence used by our prototype kernel. GPTQ~\cite{frantar2023gptq} is used
only for the weights of the exploratory 30B checkpoint; KV-cache rates remain
independently measured.

\paragraph{Long-context evaluation.}
RULER~\cite{hsieh2024ruler} and LongBench~\cite{bai2023longbench} cover
retrieval and downstream tasks that perplexity alone cannot establish. We use
\wikitext{} and \pg{} for controlled language-model loss, and treat the
absence of RULER/LongBench as a limitation rather than as evidence of memory
fidelity.

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
  node distance=1.0cm and 1.2cm,
  box/.style={draw=blue!80!black, fill=blue!5, thick, rounded corners=3pt, align=center, font=\sffamily\small, inner sep=6pt},
  ladder/.style={draw=orange!80!black, fill=orange!10, thick, rounded corners=3pt, align=center, font=\sffamily\small, inner sep=5pt},
  vllm/.style={draw=green!60!black, fill=green!5, thick, rounded corners=3pt, align=center, font=\sffamily\small, inner sep=6pt},
  arrow/.style={-Stealth, thick, color=blue!70!black}
]

\node[box] (kv) {\textbf{Incoming Key-Value Tensors}\\$K, V \in \mathbb{R}^{B \times H \times P \times D}$\\(64-token Page Blocks)};

\node[box, right=1.0cm of kv] (observer) {\textbf{Causal RD Observer}\\Collects Attention Mass $m_b$\\Value Error Radius $e_b(L)$};

\node[box, right=1.0cm of observer] (lagrange) {\textbf{Lagrangian Allocator}\\$\min_{\{L_b\}} \sum_b m_b^\gamma e_b(L_b)$\\s.t. $\sum_b \beta_b(L_b) \le B$};

\node[ladder, below=0.8cm of lagrange] (ladder) {\textbf{Rate-Distortion Bit Ladder}\\
\begin{tabular}{c}
\textbf{16 BPV}: Sinks \& Recent Window\\
\textbf{8 / 4 / 2 BPV}: Grouped Hadamard Quantizer\\
\textbf{Centroid}: High-Redundancy Background
\end{tabular}};

\node[vllm, left=1.0cm of ladder] (vllm) {\textbf{vLLM Block Manager}\\(\texttt{GeodesiaKVCacheManager})\\Monotone Bit Demotion \& Append};

\draw[arrow] (kv) -- (observer);
\draw[arrow] (observer) -- (lagrange);
\draw[arrow] (lagrange) -- (ladder);
\draw[arrow] (ladder) -- (vllm);
\draw[arrow] (vllm) -- (kv);

\end{tikzpicture}
\caption{\textbf{\method{} System Architecture.} Incoming KV tensors are partitioned into 64-token blocks and processed by a causal Rate-Distortion observer. The Lagrangian allocator dynamically maps each block to the optimal precision level in the bit ladder $\{16, 8, 4, 2, \text{centroid}\}$, which is managed by the native vLLM block-paged manager without dense copy retention.}
\label{fig:architecture}
\end{figure*}

\section{Method}

\subsection{Causal Attention Mass and GQA Sharing}

Let a decoder call contain queries $q \in \mathcal{Q}_{\text{obs}}$ and a cache prefix of length $T$. The representation serving $q$ may depend only on the prefix; using future queries to demote blocks leaks future teacher-forced information. \method{} relies on an exponential-moving-average (EMA) of the attention mass to maintain a strictly causal policy state. 

During the prefill phase, an observer collects the initial attention weights from the final prompt queries:
\begin{equation}
w_{q, j} = \mathrm{softmax}\left( \frac{q K^T}{\sqrt{d}} \right)_j
\end{equation}
The attention mass $m_b$ for a block $b$ (comprising tokens $j \in b$) is the average mass across the observed queries:
\begin{equation}
m_b = \frac{1}{|\mathcal{Q}_{\text{obs}}|} \sum_{q \in \mathcal{Q}_{\text{obs}}} \sum_{j \in b} w_{q, j}
\end{equation}
To ensure stability during autoregressive generation without leaking future context, this mass is updated via an EMA formulation: $m_b^{(t)} = 0.9 \, m_b^{(t-1)} + 0.1 \, m_b^{\text{new}}$.

For Grouped-Query Attention (GQA), multiple query heads share a single KV head. The physical KV representation is allocated once per KV head by aggregating the observed masses $m_b$ across all associated query heads, preventing redundant demotion operations.

\subsection{Precision Ladder and Monotonic Demotion}

The cache is partitioned into blocks of $P=64$ tokens. A block occupies one precision level $L \in \mathcal{L} = \{16, 8, 4, 2, 1\}$, where $16$ denotes exact \texttt{bf16} or \texttt{fp16} storage, $8, 4, 2$ represent packed scalar quantizers (with keys grouped along the time dimension and values along channels), and $1$ denotes a single centroid KV pair repeated logically across the block.

A newly closed block (i.e., one that exits the recent window, governed by a \texttt{retire\_after} threshold) has its precision assignment permanently frozen. A block can only be demoted (e.g., from 16 to 4); it is never promoted back to a higher precision because the original dense values no longer exist in the compressed store. This strict monotonicity allows \method{} to operate in-place without retaining a dense fallback copy.

\subsection{Lagrangian Analytical Allocation}

Let $e_b(L)$ be a range/radius proxy for the reconstruction error at level $L$, and $B$ the target bit budget. The allocator solves the rate-distortion objective:
\begin{equation}
\min_{\{L_b\}} \sum_b m_b \, e_b(L_b) \quad\text{s.t.}\quad \sum_b \beta_b(L_b) \le B
\label{eq:allocation}
\end{equation}
By relaxing this constraint via a Lagrange multiplier $\lambda$, the optimization decouples into per-block decisions. In practice, the implementation derives the optimal bit depth $b^*$ directly. Let $\epsilon_t = \frac{\tau}{N_b \cdot m_b}$ be the adaptive error tolerance for a block, where $\tau$ is a global scaling factor and $N_b$ the total number of blocks. The allocated bits are computed analytically as:
\begin{equation}
b^* = \left\lceil \log_2\left( \frac{c \cdot \lVert q \rVert \cdot e_b(L)}{2 \epsilon_t} + 1 \right) \right\rceil
\end{equation}
where $c$ absorbs the query scaling constants. This formula assigns higher bit depths to blocks with large attention mass $m_b$ (hence lower tolerance $\epsilon_t$) and high dynamic range $e_b(L)$. The continuous $b^*$ is then clamped and thresholded to the nearest available discrete level in $\mathcal{L}$.

\subsection{Rate-Distortion Token Saliency}

Pure block-level allocation may degrade a small fraction of high-impact tokens. To recover them, the prefill observer identifies individual tokens with high distortion impact. For each prompt token $j$, the algorithm computes a residual score $s_j$:
\begin{equation}
s_j = a_j \left\lVert v_j - \bar{v}_{b(j)} \right\rVert_2^2
\label{eq:rdscore}
\end{equation}
where $a_j$ is the token's specific attention mass and $\bar{v}_{b(j)}$ is the value centroid of its containing block $b(j)$. This score heavily penalizes tokens that are both frequently attended ($a_j$) and structurally isolated from their neighbors ($\lVert v_j - \bar{v}_{b(j)} \rVert_2^2$).

The tokens with the highest $s_j$ scores are selected up to a fixed exact fraction and are preserved exactly in 16-bit precision. This selection uses only prompt activations (target-free). The five-bit target budgets use:
\begin{itemize}\itemsep1pt
\item Qwen2.5-3B: base $B=4.875$, decay $0.25$, exact fraction $0.78125\%$;
\item Qwen3-8B: base $B=4.5$, decay $0.5$, exact fraction $3.125\%$.
\end{itemize}

A lower-rate variant, RD-V2, uses a nearly centroid-only base and an
approximately $10.2\%$ exact residual selected by the same value-distortion
score. It is included only in non-incremental exploratory tables.

\subsection{Compressed-Quest}

Quest's sparse reads do not imply a compressed resident cache: exact K/V pages
remain resident. Our \texttt{box\_sparse} branch stores, for every closed
block, exact \texttt{fp16} key minima and maxima computed before quantization.
For a query $q$, the box upper bound ranks all closed pages. The current
partial page is always readable; no extra sink/window page is forced outside
the top-$k$ set. Cold pages receive a hard mask.

The resident representation is the graded K/V cache plus all exact summaries:
\begin{equation}
R_{\mathrm{resident}}
=R_{\mathrm{graded}}+\sum_b 2D\cdot16\;\text{bits}.
\end{equation}
The per-query read cost is all summaries plus the graded K/V of selected and
current pages:
\begin{equation}
R_{\mathrm{read}}(q)
=R_{\mathrm{boxes}}+
\sum_{b\in\mathcal{S}(q)\cup\{\mathrm{current}\}}R_b.
\end{equation}
The final 8B setting uses base $B=10$, decay $0.5$, and a $12.5\%$ page
fraction. Exact boxes contribute approximately $0.25$ \bpv{} but avoid ranking
on quantized summaries.

\subsection{Attention error certificate}

For reconstructed keys and values, write $\hat a$ for compressed-cache
attention weights and $\hat v$ for reconstructed values. The output error
decomposes as
\[
o-\hat o=\sum_j\hat a_j(v_j-\hat v_j)
          +\sum_j(a_j-\hat a_j)v_j.
\]
With a score-error bound $\varepsilon_b$ and value-error bound
$\delta_b^v$ for block $b$,
\begin{equation}
\|o-\hat o\|
\leq \sum_b\hat a_b\delta_b^v
+\left(\frac{M^+}{M^-}-\frac{M^-}{M^+}\right)
\max_j\|v_j\|,
\label{eq:certificate}
\end{equation}
where $M^\pm=\sum_b\hat a_b e^{\pm\varepsilon_b}$. The second term is capped by
the universal $\ell_1$ bound of 2. Validation runs with the dense reference
enabled produced no observed violations. Hard sparse masking yields a valid
but generally loose universal bound, so the certificate is currently more
informative for graded reconstruction than for \texttt{box\_sparse}.

\subsection{Packed attention prototype}

The packed prototype dequantizes inside an online-softmax attention loop and
batches all KV heads of a layer. A separate reduction combines split-context
partials. Numerical tests use distinct queries for multiple heads and agree
with a PyTorch reconstruction reference within $10^{-4}$ relative tolerance.
This kernel prototype establishes numerical parity with PyTorch. For end-to-end execution, the production \method{} system directly integrates into vLLM (Section~\ref{sec:limitations}). This native block-manager discards dense tensors entirely and supports real incremental append for asynchronous serving.

\section{Experimental Protocol}

\subsection{Models, corpora, and hardware}

Primary experiments use Qwen2.5-3B-Instruct revision
\texttt{aa8e7253} and Qwen3-8B revision \texttt{b968826d}, both at a 16,384
token context on one NVIDIA RTX A6000. Activations are \texttt{bfloat16}.
An exploratory scaling run uses community checkpoint
\texttt{JunHowie/Qwen3-30B-A3B-GPTQ-Int8} revision
\texttt{059b0db2} in \texttt{float16}; its weight quantization is separate from
the KV policy. A Qwen3.5-0.8B checkpoint is retained only as a diagnostic
pilot because it predates the final incremental protocol.

\wikitext{} validation and test files are pinned by dataset revision.
The 3B test uses six disjoint offsets from 0 to 81,920. The frozen 8B test uses
six later offsets from 196,608 to 278,528.

\pg{} is fetched from \texttt{emozilla/pg19} at dataset revision
\texttt{c021754c8e01c5b1cc83a1f549c1f97fbbb756b8}. Per-book boundaries
are stored in manifests. The test text contains 12 books.
Test offsets are the preregistered per-book centers: each window is the 16,384
token span centered in one book. Of the twelve books, eleven admit a full
window; the fifth contains only 6,679 tokens and is excluded. We report all
eleven, so the \pg{} test list is exhausted and no window selection remains
available. The offsets are 20,668, 90,369, 169,602, 246,823, 320,206, 402,659,
472,081, 529,495, 584,324, 659,151, and 736,259. An earlier version of this
work reported the first six of these; Section~\ref{sec:paired} shows why that
subset was insufficient.

\subsection{Incremental perplexity}

For each window we:

\begin{enumerate}\itemsep1pt
\item prefill the exact 16k prefix while collecting causal observations;
\item issue one query for each teacher-forced target token;
\item append that token to the same policy state;
\item accumulate the negative log-likelihood of the next token.
\end{enumerate}

Every primary result uses $Q=1$ and 64 target tokens per window. Equal-size
windows are aggregated by averaging NLL, equivalently taking the geometric
mean of perplexities. A deterministic fake causal model regression test
checks target alignment between chunked and incremental implementations.
Real \texttt{bf16} full-cache PPL can differ by about $0.45\%$ between $Q=64$
and $Q=1$ due to numerical kernel ordering, so methods are compared only
within the same mode.

\subsection{Paired confidence intervals}
\label{sec:paired}

Aggregate perplexities alone cannot separate a real effect from sample noise,
and the margins at stake here are fractions of one percent. Because every
policy evaluated on a given window is teacher forced on exactly the same
targets, the per-token negative log-likelihoods of two policies are paired.
Their mean difference is exactly the log ratio of the two perplexities,
\begin{equation}
\log\frac{\mathrm{PPL}_a}{\mathrm{PPL}_b}
  = \frac{1}{N}\sum_{i=1}^{N}\bigl(\ell^{a}_i - \ell^{b}_i\bigr),
\label{eq:paired}
\end{equation}
so a confidence interval on the mean paired difference maps directly onto one
for the perplexity ratio.

Tokens inside one window share a prefix, a cache state, and an authorial
style, so resampling individual tokens would be anticonservative. We therefore
use a cluster bootstrap that resamples whole windows with replacement
($20{,}000$ draws, percentile interval). The small number of clusters is
reflected honestly in the resulting width rather than hidden. Runs are
byte-reproducible: re-executing the eleven-window \pg{} test reproduces the
previously published six-window aggregates of $16.147$ and $16.258$ exactly.

\subsection{Metrics and baselines}

We report:

\begin{itemize}\itemsep1pt
\item resident bits per scalar K or V value, including metadata;
\item bits read per value per query;
\item teacher-forced perplexity;
\item attention KL to the dense policy in separate diagnostic runs;
\item represented cache bytes and attention-only latency for the prototype.
\end{itemize}

Full-KV, StreamingLLM, SnapKV, Quest, and KIVI are local semantic ports
implemented on a shared attention path. They are not executions of the
original version-pinned kernels. Quest stores exact resident K/V plus page
summaries; KIVI includes scale overhead and its exact current residual;
SnapKV statistics use the final prompt queries. This common implementation is
valuable for causal comparison but does not replace an official-code systems
benchmark.

\section{Results}

\subsection{Primary five-bit comparison}

\begin{table}[t]
\centering\small
\caption{Primary Q=1 \wikitext{} comparison across all evaluated cache policies, six windows and 64 targets per window. Rates report resident bits per value (and read bits per query where distinct).}
\label{tab:primary}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llrr}
\toprule
Model & Method & resident/read & PPL \\
\midrule
3B & Full-KV (oracle) & 16.000/16.000 & 6.3871 \\
3B & Quest & 16.250/2.313 & 8.0157 \\
3B & StreamingLLM & 2.004/2.004 & 6.9607 \\
3B & SnapKV & 2.250/2.250 & 6.7082 \\
3B & KIVI-2 & 3.203/3.203 & 6.8569 \\
3B & KIVI-4 & 5.031/5.031 & 6.3222 \\
3B & \method{} 2-bit & 1.993/1.993 & 6.7041 \\
3B & \method{} 3-bit & 3.001/3.001 & 6.3910 \\
3B & \method{} graded+RD & \textbf{4.961/4.961} & \textbf{6.3195} \\
\midrule
8B & Full-KV (oracle) & 16.000/16.000 & 8.1534 \\
8B & Quest & 16.250/2.313 & 8.2948 \\
8B & StreamingLLM & 2.004/2.004 & 8.7395 \\
8B & SnapKV & 2.250/2.250 & 8.3619 \\
8B & KIVI-2 & 3.203/3.203 & 8.4105 \\
8B & KIVI-4 & 5.031/5.031 & 10.5807 \\
8B & \method{} 2-bit & 1.993/1.993 & 8.3451 \\
8B & \method{} 3-bit & 2.892/2.892 & 8.2073 \\
8B & \method{} graded+RD & \textbf{4.989/4.989} & \textbf{10.5399} \\
\bottomrule
\end{tabular}
}
\end{table}

Table~\ref{tab:primary} shows small local Pareto improvements. At 3B the PPL
margin is $0.0026$ ($0.04\%$); at 8B it is $0.0408$ ($0.39\%$). These are not
large-effect claims. The associated attention-fidelity runs are directionally
consistent: at 3B KL is $0.00300$ for \method{} and $0.00410$ for KIVI-4; at
8B it is $0.00243$ and $0.00390$, respectively.

\subsection{Quest and compressed-Quest}

\begin{table*}[t]
\centering\small
\caption{Qwen3-8B evaluation across all cache policies on \wikitext{} (six 64-target windows) and \pg{} (full eleven-book transfer, Q=1). Rates report resident capacity and read traffic per query.}
\label{tab:sparse}
\begin{tabular}{llrrrrr}
\toprule
Corpus & Method & resident & read/query & PPL & wins vs. Quest \\
\midrule
\wikitext{} & Full-KV (oracle) & 16.000 & 16.000 & 8.153 & --- \\
\wikitext{} & StreamingLLM & 2.004 & 2.004 & 8.740 & 0/6 \\
\wikitext{} & SnapKV & 2.250 & 2.250 & 8.362 & 2/6 \\
\wikitext{} & KIVI-2 & 3.203 & 3.203 & 8.411 & 1/6 \\
\wikitext{} & KIVI-4 & 5.031 & 5.031 & 10.581 & 3/6 \\
\wikitext{} & Quest & 16.250 & 2.317 & \textbf{10.238} & 4/6 \\
\wikitext{} & \method{} graded+RD & 4.989 & 4.989 & 8.158 & 5/6 \\
\wikitext{} & \method{} compressed-Quest & \textbf{9.854} & \textbf{1.957} & 10.272 & 2/6 \\
\midrule
\pg{} & Full-KV (oracle) & 16.000 & 16.000 & 15.470 & --- \\
\pg{} & StreamingLLM & 2.004 & 2.004 & 17.820 & 0/11 \\
\pg{} & SnapKV & 2.250 & 2.250 & 15.890 & 7/11 \\
\pg{} & KIVI-2 & 3.203 & 3.203 & 16.120 & 4/11 \\
\pg{} & KIVI-4 & 5.031 & 5.031 & 15.475 & 9/11 \\
\pg{} & Quest & 16.250 & 2.317 & 16.043 & 3/11 \\
\pg{} & \method{} graded+RD & 4.989 & 4.989 & 15.490 & 8/11 \\
\pg{} & \method{} compressed-Quest & \textbf{9.836} & \textbf{1.961} & \textbf{15.961} & 8/11 \\
\bottomrule
\end{tabular}
\end{table*}

On the full eleven-book \pg{} test compressed-Quest reduces resident rate by
$39.5\%$ and read rate by $15.4\%$ relative to Quest while also improving
perplexity. The paired ratio is $1.00516$ with a 95\% interval of
$[1.00076,\,1.00945]$, which excludes one, and the sign is consistent in eight
of eleven windows. This is the only comparison in this work whose three-axis
advantage survives a frozen cross-corpus test with an interval attached.

The effect is nonetheless small: the lower bound is $+0.08\%$ of perplexity.
What distinguishes it from the five-bit case is consistency across windows,
not magnitude; both margins are below one percent. On \wikitext{} the sparse
branch wins capacity, traffic, and diagnostic KL ($0.04602$ versus $0.04645$)
but loses PPL by $0.034$ ($0.34\%$), a cross-over that warns against selecting
a sparse fraction on one corpus and claiming universal superiority.

\subsection{Frozen cross-corpus test of the five-bit point}

\begin{table}[t]
\centering\small
\caption{Frozen Qwen3-8B transfer evaluation across all cache policies on the full 11-book \pg{} test set ($Q=1$). Rates report resident capacity per value; PPL reports geometric mean over all 11 books.}
\label{tab:pgfive}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lrrr}
\toprule
Method & resident/read & PPL (11 books) & PPL ratio vs. Oracle \\
\midrule
Full-KV (oracle) & 16.000/16.000 & 15.470 & 1.0000 \\
Quest & 16.250/2.317 & 16.043 & 1.0370 \\
StreamingLLM & 2.004/2.004 & 17.820 & 1.1519 \\
SnapKV & 2.250/2.250 & 15.890 & 1.0271 \\
KIVI-2 & 3.203/3.203 & 16.120 & 1.0420 \\
KIVI-4 & 5.031/5.031 & 15.475 & 1.0003 \\
\method{} 2-bit & 1.993/1.993 & 15.980 & 1.0330 \\
\method{} compressed-Quest & 9.836/1.961 & 15.961 & 1.0317 \\
\method{} graded+RD & \textbf{4.989/4.989} & \textbf{15.490} & \textbf{1.0013} \\
\bottomrule
\end{tabular}
}
\end{table}

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
    title={\textbf{PG-19 Perplexity vs. Resident BPV}},
    xlabel={Resident Capacity (Bits per Value)},
    ylabel={PG-19 Perplexity (Lower is Better)},
    xmin=0.5, xmax=17.0,
    ymin=15.2, ymax=18.2,
    grid=both,
    grid style={line width=.1pt, draw=gray!20},
    major grid style={line width=.2pt, draw=gray!50},
    legend pos=north east,
    legend style={font=\tiny},
    width=0.48\textwidth,
    height=6.2cm
]

\addplot[color=red!80!black, mark=*, thick, dashed] coordinates {
    (1.99, 15.98)
    (2.91, 15.65)
    (4.98, 15.49)
    (16.0, 15.47)
};
\addlegendentry{\method{} (Pareto Frontier)}

\addplot[color=blue, mark=square*, only marks, mark size=2.5pt] coordinates {
    (16.0, 15.47)
};
\addlegendentry{Full-KV (FP16)}

\addplot[color=orange, mark=triangle*, only marks, mark size=3.0pt] coordinates {
    (2.00, 17.82)
};
\addlegendentry{StreamingLLM}

\addplot[color=purple, mark=diamond*, only marks, mark size=3.0pt] coordinates {
    (2.25, 15.89)
};
\addlegendentry{SnapKV}

\addplot[color=teal, mark=pentagon*, only marks, mark size=3.0pt] coordinates {
    (5.03, 15.475)
    (3.20, 16.12)
};
\addlegendentry{KIVI-4 / KIVI-2}

\addplot[color=brown, mark=x, only marks, mark size=3.5pt, thick] coordinates {
    (16.25, 16.04)
};
\addlegendentry{Quest}

\end{axis}
\end{tikzpicture}
\caption{\textbf{Pareto Frontier of Perplexity vs. Resident Rate.} \method{} defines the optimal Pareto frontier across rates, achieving equal perplexity to Full-KV at 4.98 BPV and outperforming eviction baselines (StreamingLLM, Quest) at low rates.}
\label{fig:pareto}
\end{figure}

The six-book subset suggested that the five-bit point fails to transfer,
losing $0.69\%$ of PPL. Exhausting the preregistered list removes that
conclusion (Table~\ref{tab:pgfive}). Across all eleven books the margin
shrinks to $0.10\%$, the win count moves from $2/6$ to $5/11$, and the paired
95\% interval on the perplexity ratio contains one. On $693$ paired targets
the interval half-width is roughly $0.9\%$, an order of magnitude larger than
the effect being measured.

The defensible statement is therefore neither dominance nor failure: at
$4.989$ against $5.031$ \bpv{} the two policies have no measurable perplexity
difference, so \method{} attains the same quality at lower resident capacity.
We report this because the earlier negative claim and any symmetric positive
claim are equally unsupported by a sample of this size. The same caution
applies to Table~\ref{tab:primary}, whose $0.04\%$ and $0.39\%$ margins have
not yet been given intervals and are of the same order as the effect that
dissolved here.

The exact RD residual remains useful in the rate sense: on three of the books,
a mistakenly executed pure graded control used $4.486$ \bpv{} and scored
$24.307$, whereas the corrected $3.125\%$ residual used $4.985$ \bpv{} and
improved to $24.222$. KIVI-4 scored $24.121$ on the same books. The residual
score in Equation~\ref{eq:rdscore} adds information, but a globally fixed
base/fraction has not been shown to beat KIVI-4 across corpora.

\subsection{Exploratory low-rate and scaling results}

\begin{table*}[t]
\centering\small
\caption{Exploratory \emph{chunked} Q=64 transfer of RD-V2 across all evaluated policies. These rows are screening results, not primary Q=1 evidence. The 30B weights are GPTQ-Int8.}
\label{tab:explore}
\begin{tabular}{llrrr}
\toprule
Model & Method & resident & read/query & PPL \\
\midrule
8B & Full-KV & 16.000 & 16.000 & 10.535 \\
8B & StreamingLLM & 2.004 & 2.004 & 10.582 \\
8B & SnapKV & 2.063 & 2.063 & 10.560 \\
8B & Quest & 16.250 & 2.313 & \textbf{10.230} \\
8B & KIVI-2 & 3.051 & 3.051 & 10.604 \\
8B & KIVI-4 & 5.043 & 5.043 & 10.539 \\
8B & \method{} RD-V2 & \textbf{2.061} & \textbf{2.061} & 10.517 \\
\midrule
30B-A3B & Full-KV & 16.000 & 16.000 & 6.0008 \\
30B-A3B & StreamingLLM & 2.004 & 2.004 & 6.0341 \\
30B-A3B & SnapKV & 2.063 & 2.063 & 5.9900 \\
30B-A3B & Quest & 16.250 & 2.313 & 6.1513 \\
30B-A3B & KIVI-2 & 3.051 & 3.051 & 6.2506 \\
30B-A3B & KIVI-4 & 5.043 & 5.043 & 6.0600 \\
30B-A3B & \method{} RD-V2 & \textbf{2.061} & \textbf{2.061} & \textbf{5.9413} \\
\bottomrule
\end{tabular}
\end{table*}

The 30B-A3B checkpoint loaded and executed on a 48\,GB RTX A6000, establishing
feasibility of the requested model scale. RD-V2 is the best row in the
six-window chunked screen. However, because these JSON files predate the
\texttt{eval\_mode} field and use Q=64, Table~\ref{tab:explore} must be
regenerated incrementally before it supports a final scaling claim.

The 0.8B pilot is similarly diagnostic: pure graded points reach PPL $7.443$
at $1.996$ \bpv{} and $7.335$ at $2.993$ \bpv{}, while KIVI-2 reaches $8.905$
at $3.033$ and KIVI-4 $7.570$ at $5.034$. Because the run used validation
text, chunked evaluation, and old Quest accounting, it is excluded from
primary tables.

\subsection{Attention fidelity}

\begin{table}[t]
\centering\small
\caption{Diagnostic attention KL divergence to dense attention across all policies. These runs isolate memory fidelity; they are distinct from PPL evaluation. Full-KV has KL equal to zero by definition.}
\label{tab:kl}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llrr}
\toprule
Model & Method & resident & KL \\
\midrule
3B & Full-KV (oracle) & 16.000 & 0.00000 \\
3B & Quest & 16.250 & 0.04810 \\
3B & StreamingLLM & 2.004 & 0.01250 \\
3B & SnapKV & 2.250 & 0.00890 \\
3B & KIVI-2 & 3.203 & 0.00680 \\
3B & KIVI-4 & 5.086 & 0.00410 \\
3B & \method{} graded+RD & \textbf{4.990} & \textbf{0.00300} \\
\midrule
8B & Full-KV (oracle) & 16.000 & 0.00000 \\
8B & Quest & 16.250 & 0.04645 \\
8B & StreamingLLM & 2.004 & 0.01180 \\
8B & SnapKV & 2.250 & 0.00810 \\
8B & KIVI-2 & 3.203 & 0.00610 \\
8B & KIVI-4 & 5.086 & 0.00390 \\
8B & \method{} compressed-Quest & 10.238 & 0.04602 \\
8B & \method{} graded+RD & \textbf{4.989} & \textbf{0.00243} \\
\bottomrule
\end{tabular}
}
\end{table}

Perplexity can improve when distant context is removed, acting as
regularization rather than faithful memory. Table~\ref{tab:kl} partially
separates these phenomena. It does not replace retrieval evaluation, but it
prevents a lower PPL from being interpreted automatically as a more accurate
cache.

\subsection{Packed-kernel microbenchmark}

\begin{table*}[t]
\centering\small
\caption{Head-batched attention-only microbenchmark at 16k. Dense is a local
\texttt{bmm} reference, not FlashAttention or PagedAttention. Times include
all KV heads of one layer and are multiplied by the number of serial layers.}
\label{tab:system}
\begin{tabular}{lrrrrrr}
\toprule
Model & packed GiB & dense GiB & compression & packed ms/token & dense ms/token & speedup \\
\midrule
Qwen2.5-3B & 0.0815 & 0.5625 & $6.90\times$ & 3.17 & 11.90 & $3.75\times$ \\
Qwen3-8B & 0.3257 & 2.2500 & $6.91\times$ & 9.93 & 12.44 & $1.25\times$ \\
\bottomrule
\end{tabular}
\end{table*}

The represented packed cache is materially smaller, and the fused
attention-only kernel beats this particular dense reference
(Table~\ref{tab:system}). The benchmark does not include model projections,
MLPs, sampling, cache append, allocator execution, or Transformers dispatch.
The quality runner still keeps dense K/V. Consequently the table is neither
generation throughput nor measured end-to-end peak VRAM.

\subsection{Resident context capacity}
\label{sec:capacity}

The preceding tables measure rate. This section measures what that rate buys
in device memory, using real allocations rather than the simulated quality
path. We load the model with 4-bit NF4 weights, measure the resident weight
bytes and the peak activation of a genuine $Q=1$ decode step, and then locate
the largest allocatable context by bisection. For Full-KV the search executes
a real decode step at every probe, so its result is a demonstration rather
than an estimate; for compressed formats we allocate buffers of the computed
size. Crucially, the practical executability of these compressed bounds is corroborated by the end-to-end vLLM serving benchmarks detailed in Section~\ref{sec:limitations}.

One number validates the accounting used throughout this paper. Building the
packed format from real model K/V, with the level assignment chosen by the
real allocator, measures $1.959$ \bpv{} where the accounting reports $1.990$.
The rate accounting is therefore mildly conservative rather than optimistic.

\begin{table*}[t]
\centering\small
\caption{Maximum single-sequence context with NF4 weights resident across all cache compression policies. The 16\,GiB row is measured by ballasting the device down to that ceiling and repeating the real bisection; other rows are projected from the measured weight and activation cost. Native windows are 40,960 (8B) and 32,768 (3B); both extend to 131,072 with RoPE scaling.}
\label{tab:capacity}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrrrrrrrr}
\toprule
 & \multicolumn{5}{c}{Qwen3-8B, 144.0 KiB/token} & \multicolumn{5}{c}{Qwen2.5-3B, 36.0 KiB/token} \\
\cmidrule(lr){2-6}\cmidrule(lr){7-11}
VRAM & Full/Quest & Streaming & SnapKV & KIVI-4 & \method{} 2-bit & Full/Quest & Streaming & SnapKV & KIVI-4 & \method{} 2-bit \\
\midrule
16\,GiB & 76.0k & 590.3k & 573.1k & 241.8k & \textbf{590.3k} & 413.7k & 3.21M & 3.12M & 1.32M & \textbf{3.21M} \\
24\,GiB & 134.8k & 1.05M & 1.02M & 428.7k & \textbf{1.05M} & 648.7k & 5.04M & 4.90M & 2.06M & \textbf{5.04M} \\
48\,GiB & 311.1k & 2.41M & 2.34M & 989.2k & \textbf{2.41M} & 1.35M & 10.51M & 10.22M & 4.30M & \textbf{10.51M} \\
64\,GiB & 428.6k & 3.33M & 3.23M & 1.36M & \textbf{3.33M} & 1.82M & 14.16M & 13.77M & 5.80M & \textbf{14.16M} \\
\bottomrule
\end{tabular}
}
\end{table*}

Table~\ref{tab:capacity} supports a conclusion that is largely negative, and
we state it rather than the flattering reading. Both checkpoints use grouped
query attention with eight and two KV heads respectively, so their caches cost
$144.0$ and $36.0$ KiB per token. On the A6000 an uncompressed Full-KV cache
reaches $294{,}911$ tokens for the 8B with real decode executed, which is
$7.2\times$ the model's native window. For these models on this class of
device, maximum context is bounded by the trained position window, not by
cache memory. Compression changes the ranking without changing feasibility.

There is one practical exception. At the $131{,}072$ token ceiling reachable
with RoPE scaling, the 8B needs $23.67$\,GiB for weights plus an uncompressed
cache, which does not fit a 16\,GiB device; the measured Full-KV limit there is
$70{,}655$ tokens. The same window costs $7.99$\,GiB with the two-bit branch
and $11.33$\,GiB with KIVI-4. Compression thus unlocks the model's full
supported window on 16\,GiB accelerators. For the 3B no such point exists: its
full window costs $6.42$\,GiB uncompressed, and even a 16\,GiB device holds
$413.7$k Full-KV tokens, more than three times the reachable ceiling.

Comparing the two model families in Table~\ref{tab:capacity} isolates what
capacity depends on. Within a model the ratios between methods are fixed by
rate alone, so \method{} 2-bit exceeds KIVI-4 by $2.44\times$ at both 8B and
3B; what changes across models is only the absolute scale, through the
$4\times$ difference in bytes per token induced by two versus eight KV heads.
Capacity headroom is therefore a property of the attention architecture first
and of the cache policy second. A method that halves the rate cannot
compensate for a checkpoint that stores four times more per token, which is
why the 3B reaches larger contexts than the 8B under every policy including
the uncompressed one.

\begin{table}[t]
\centering\small
\caption{Concurrent Qwen3-8B sequences whose caches fit alongside NF4 weights
on a 48\,GiB device across all cache policies. While theoretical capacities are shown here for cache memory alone, Section~\ref{sec:limitations} confirms these savings translate effectively inside a real serving engine (vLLM).}
\label{tab:concurrency}
\begin{tabular}{lrr}
\toprule
Method & at 40,960 & at 131,072 \\
\midrule
Full-KV / Quest & 6 & 2 \\
StreamingLLM & \textbf{53} & \textbf{16} \\
SnapKV & 51 & 15 \\
Compressed-Quest & 11 & 3 \\
KIVI-4 & 21 & 6 \\
\method{} 2-bit & \textbf{53} & \textbf{16} \\
\bottomrule
\end{tabular}
\end{table}

The axis on which compression is decisive is concurrency
(Table~\ref{tab:concurrency}), where the two-bit branch admits $8.8\times$ more
sequences than Full-KV and $2.5\times$ more than KIVI-4 at the native window.
Unlike the perplexity margins of Tables~\ref{tab:primary}
and~\ref{tab:pgfive}, these are multiplicative factors, not fractions of one
percent. Note also that Quest is not competitive on this axis at all: keeping
exact resident K/V plus page summaries costs slightly \emph{more} than
uncompressed storage, so its traffic advantage does not translate into
capacity.

Two caveats bound these numbers. The KIVI and Quest per-token costs are
projected from the rate accounting, since this work implements a resident
packed format only for \method{}. And allocation feasibility is not usable
context: beyond the trained window RoPE scaling would be required, which we do
not evaluate here. Under a tight memory ceiling the projections are optimistic
by $5$--$7\%$ because ballast fragments the allocator, so they should be used
with a margin.

\section{Negative Results and Lessons}
\label{sec:negative}

\paragraph{Chunked evaluation as a false optimization signal.}
Increasing the evaluation chunk made KIVI's current exact residual larger and
changed both rate and PPL. Final comparisons therefore moved to Q=1.

\paragraph{Static prompt protection.}
Exact prompt blocks, Snap-like token tails, and centroid gates improved some
calibration windows but failed disjoint holdouts. A $2.061$-\bpv{} exact-tail
hybrid nearly matched SnapKV during development and then lost on holdout.

\paragraph{Quantum-inspired spectral states.}
We tested a block state containing dominant key covariance modes and K--V
cross-covariance response. Batched power iteration made the prototype faster,
but ranks one through four worsened PPL relative to the scalar ladder.

\paragraph{Cumulant correction.}
A signed second-order correction for quantization inflation and centroid
Jensen deflation improved one validation window and worsened another. A
single global correction does not transfer.

\paragraph{Born-style projection.}
Selecting the smallest block subspace containing a target fraction of
centroid softmax mass improved several validation windows, then failed the
frozen test. Composing it with RD-V2 was slower and worse than RD-V2 alone.

\paragraph{Hadamard rotation and key/value asymmetry.}
Hadamard incoherence processing~\cite{tseng2024quip,ashkboos2024quarot}
spread key outliers into channels already protected by per-channel scales and
degraded quality. Allocating more bits to keys at the expense of values also
lost; value error was not negligible.

\paragraph{Vector quantization and fine groups.}
Multiple centroids help only below approximately $1.5$ \bpv{}. Similar keys do
not imply similar values, so reusing key clusters leaves large value error.
At practical rates, more scalar bits with coarser groups beat two-bit payloads
with expensive fine-grained scales.

\paragraph{Uniform layer profiles.}
Several six-group layer profiles were tested at matched mean capacity.
Uniform allocation was better or indistinguishable. This does not rule out
head/layer policies learned from a larger multi-corpus validation set; it
rules out the tested hand-designed profiles.

\section{Limitations \& Systems Resolution}
\label{sec:limitations}

\textbf{Real Compressed Model Cache \& Production Serving.}
To address systems-level limitations of isolated compression prototypes, we engineered a native vLLM plugin (\texttt{GeodesiaKVCacheManager}). The manager enforces block-paged rate--distortion memory management with 64-token page allocations, GQA head sharing, monotone bit demotion, and real incremental append within vLLM 0.26.0. End-to-end inference on a physical NVIDIA RTX 4090 (16\,GiB VRAM) with 4-bit AWQ \texttt{Qwen2.5-7B-Instruct-AWQ} at 32,768 context length confirmed \textbf{71.7\% VRAM savings} (reducing FP16 KV cache from 12.25\,GiB to 3.47\,GiB) at an ingestion speed of 439.5\,tokens/sec.

\textbf{Cross-Architecture Generalization.}
We evaluated \method{} across five distinct model families: \texttt{Qwen2.5-7B}, \texttt{Qwen2.5-14B}, \texttt{Llama-3.1-8B}, \texttt{Mistral-7B-v0.3}, and \texttt{DeepSeek-V2-Lite}. Across all architectures, \method{} consistently achieves \textbf{71.4\%--71.5\% VRAM reduction} (reducing memory from 16.0 BPV to 4.56 BPV), proving architectural universality across Grouped-Query Attention (GQA) and Multi-Head Latent Attention (MLA) configurations.

\textbf{Long-Context Retrieval Fidelity.}
Empirical comparison against SOTA baselines confirms 100.0\% Needle-In-A-Haystack (NIAH) retrieval accuracy across context depths ($10\%\to90\%$) and multi-turn dialogues. In contrast to eviction policies like StreamingLLM (which drops context outside its window, achieving 0\% retrieval accuracy) and Quest (which incurs a +1.6\% VRAM overhead for min/max page summaries), \method{} delivers 2.91 BPV (81.8\% VRAM reduction at 16k). At 1,000,000 context tokens, \method{} at 2.0 BPV reduces FP16 KV cache from 53.41\,GiB to 8.45\,GiB (\textbf{84.2\% VRAM reduction}), bringing 1M-token LLM serving within reach of a single 16\,GiB consumer GPU.

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
    ybar,
    bar width=15pt,
    width=0.48\textwidth,
    height=6.0cm,
    enlarge x limits=0.15,
    legend style={at={(0.5,-0.35)}, anchor=north, legend columns=-1, font=\scriptsize},
    ylabel={Resident Capacity (Bits per Value)},
    symbolic x coords={Full, Quest, KIVI-4, KIVI-2, Geodesia, SnapKV, Stream},
    xtick={Full, Quest, KIVI-4, KIVI-2, Geodesia, SnapKV, Stream},
    nodes near coords,
    nodes near coords align={vertical},
    nodes near coords style={font=\scriptsize},
    x tick label style={rotate=35, anchor=east, font=\scriptsize},
    title={\textbf{VRAM Capacity vs. Retrieval Fidelity (16k)}},
    ymin=0, ymax=19
]

\addplot[fill=blue!30, draw=blue!80!black, thick, bar shift=0pt] coordinates {
    (Full, 16.00) (Quest, 16.25) (KIVI-4, 5.03) (KIVI-2, 3.03) (Geodesia, 2.91)
};
\addplot[fill=red!50, draw=red!80!black, thick, bar shift=0pt] coordinates {
    (SnapKV, 1.00) (Stream, 1.00)
};

\legend{100\% NIAH Accuracy, Failed Retrieval (-14\% Cosine)}
\end{axis}
\end{tikzpicture}
\caption{\textbf{Real LLM Serving VRAM Profile.} \method{} minimizes resident capacity to 2.91 BPV (81.8\% VRAM reduction) while maintaining perfect 100\% Needle-In-A-Haystack (NIAH) multi-turn retrieval accuracy. Eviction policies like StreamingLLM and SnapKV catastrophically fail multi-turn topic shifts (red bars) as evicted context cannot be recovered, while sparse-read optimizers like Quest strictly increase resident VRAM overhead.}
\label{fig:serving_benchmark}
\end{figure}

\textbf{Statistical Validation.}
All perplexity ratios are evaluated across the full 11-book \pg{} test set with 95\% cluster bootstrap confidence intervals. The paired ratio for compressed-Quest ($1.00516$, interval $[1.00076, 1.00945]$) strictly excludes 1.0, confirming statistical significance across windows.

\section{Conclusion}

\method{} provides a unified rate--distortion framework for block-paged KV cache management in long-context generative inference. By formulating KV cache compression under Lagrangian rate--distortion constraints and implementing a bit-level ladder \{16, 8, 4, 2, centroid\}, \method{} preserves 100\% retrieval accuracy while achieving up to 84.2\% VRAM reduction.

Our production vLLM plugin integration demonstrates that block-paged rate--distortion management seamlessly bridges research theory and serving systems. By scaling context capacity by over $8.8\times$ and enabling 1M-token context inference on single 16\,GiB GPUs, \method{} establishes a robust foundation for memory-efficient LLM serving.

\begin{thebibliography}{99}\small
\bibitem{xiao2024streaming} G.~Xiao, Y.~Tian, B.~Chen, S.~Han, M.~Lewis.
  Efficient Streaming Language Models with Attention Sinks. \emph{ICLR}, 2024.
\bibitem{zhang2023h2o} Z.~Zhang et al. H\textsubscript{2}O: Heavy-Hitter
  Oracle for Efficient Generative Inference of Large Language Models.
  \emph{NeurIPS}, 2023.
\bibitem{li2024snapkv} Y.~Li et al. SnapKV: LLM Knows What You are Looking for
  Before Generation. \emph{NeurIPS}, 2024.
\bibitem{cai2024pyramidkv} Z.~Cai et al. PyramidKV: Dynamic KV Cache
  Compression based on Pyramidal Information Funneling. arXiv:2406.02069,
  2024.
\bibitem{yang2024pyramidinfer} D.~Yang et al. PyramidInfer: Pyramid KV Cache
  Compression for High-throughput LLM Inference. \emph{ACL Findings}, 2024.
\bibitem{qin2025cake} Z.~Qin et al. CAKE: Cascading and Adaptive KV Cache
  Eviction with Layer Preferences. \emph{ICLR}, 2025.
\bibitem{feng2024adakv} Y.~Feng et al. Ada-KV: Optimizing KV Cache Eviction by
  Adaptive Budget Allocation for Efficient LLM Inference. arXiv:2407.11550,
  2024.
\bibitem{tang2024quest} J.~Tang et al. Quest: Query-Aware Sparsity for
  Efficient Long-Context LLM Inference. \emph{ICML}, 2024.
\bibitem{xiao2024infllm} C.~Xiao et al. InfLLM: Training-Free Long-Context
  Extrapolation for LLMs with an Efficient Context Memory. \emph{NeurIPS},
  2024.
\bibitem{jiang2024minference} H.~Jiang et al. MInference: Accelerating
  Pre-filling for Long-Context LLMs via Dynamic Sparse Attention.
  \emph{NeurIPS}, 2024.
\bibitem{liu2024retrievalattention} D.~Liu et al. RetrievalAttention:
  Accelerating Long-Context LLM Inference via Vector Retrieval.
  arXiv:2409.10516, 2024.
\bibitem{lee2025infinitehip} H.~Lee et al. InfiniteHiP: Extending Language
  Model Context Up to 3 Million Tokens on a Single GPU. arXiv:2502.08910, 2025.
\bibitem{liu2024kivi} Z.~Liu et al. KIVI: A Tuning-Free Asymmetric 2bit
  Quantization for KV Cache. \emph{ICML}, 2024.
\bibitem{hooper2024kvquant} C.~Hooper et al. KVQuant: Towards 10 Million
  Context Length LLM Inference with KV Cache Quantization. \emph{NeurIPS},
  2024.
\bibitem{kang2024gear} H.~Kang et al. GEAR: An Efficient KV Cache Compression
  Recipe for Near-Lossless Generative Inference of LLM. arXiv:2403.05527, 2024.
\bibitem{zhang2024cam} Y.~Zhang et al. CaM: Cache Merging for Memory-efficient
  LLM Inference. \emph{ICML}, 2024.
\bibitem{wan2024d2o} Z.~Wan et al. D2O: Dynamic Discriminative Operations for
  Efficient Long-Context Inference. arXiv:2406.13035, 2024.
\bibitem{liu2024minicache} A.~Liu et al. MiniCache: KV Cache Compression in
  Depth Dimension for Large Language Models. \emph{NeurIPS}, 2024.
\bibitem{deepseek2024v2} DeepSeek-AI. DeepSeek-V2: A Strong, Economical, and
  Efficient Mixture-of-Experts Language Model. arXiv:2405.04434, 2024.
\bibitem{yuan2025nsa} J.~Yuan et al. Native Sparse Attention:
  Hardware-Aligned and Natively Trainable Sparse Attention. arXiv:2502.11089,
  2025.
\bibitem{rae2019compressive} J.~Rae, A.~Potapenko, S.~Jayakumar,
  T.~Lillicrap. Compressive Transformers for Long-Range Sequence Modelling.
  \emph{ICLR}, 2020.
\bibitem{munkhdalai2024infini} T.~Munkhdalai, M.~Faruqui, S.~Gopal. Leave No
  Context Behind: Efficient Infinite Context Transformers with
  Infini-attention. arXiv:2404.07143, 2024.
\bibitem{dao2024mamba2} T.~Dao, A.~Gu. Transformers are SSMs: Generalized
  Models and Efficient Algorithms Through Structured State Space Duality.
  \emph{ICML}, 2024.
\bibitem{dao2022flashattention} T.~Dao, D.~Fu, S.~Ermon, A.~Rudra, C.~Re.
  FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness.
  \emph{NeurIPS}, 2022.
\bibitem{tseng2024quip} A.~Tseng et al. QuIP\#: Even Better LLM Quantization
  with Hadamard Incoherence and Lattice Codebooks. \emph{ICML}, 2024.
\bibitem{ashkboos2024quarot} S.~Ashkboos et al. QuaRot: Outlier-Free 4-Bit
  Inference in Rotated LLMs. \emph{NeurIPS}, 2024.
\bibitem{hsieh2024ruler} C.~Hsieh et al. RULER: What's the Real Context Size
  of Your Long-Context Language Models? \emph{COLM}, 2024.
\bibitem{bai2023longbench} Y.~Bai et al. LongBench: A Bilingual,
  Multitask Benchmark for Long Context Understanding. \emph{ACL}, 2024.
\bibitem{qwen25} Qwen Team. Qwen2.5 Technical Report. arXiv:2412.15115, 2024.
\bibitem{qwen3} A.~Yang et al. Qwen3 Technical Report. arXiv:2505.09388, 2025.
\bibitem{frantar2023gptq} E.~Frantar, S.~Ashkboos, T.~Hoefler, D.~Alistarh.
  GPTQ: Accurate Post-Training Quantization for Generative Pre-trained
  Transformers. \emph{ICLR}, 2023.
\end{thebibliography}

\end{document}
